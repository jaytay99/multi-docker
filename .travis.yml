sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t jbtaylo/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true jbtaylo/react-test npm test

after_success:
  - docker build -t jbtaylo/multi-client ./client
  - docker build -t jbtaylo/multi-nginx ./nginx
  - docker build -t jbtaylo/multi-server ./server
  - docker build -t jbtaylo/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push jbtaylo/multi-client
  - docker push jbtaylo/multi-nginx
  - docker push jbtaylo/multi-server
  - docker push jbtaylo/multi-worker
